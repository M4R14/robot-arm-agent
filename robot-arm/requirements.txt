fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
pybullet==3.2.6
numpy==1.26.4
loguru==0.7.2
cachetools==7.1.7
# Not used by sim's own FastAPI app code — only by the small Python
# snippets watch-arm.py injects via `docker compose exec sim python3 -c
# "..."` (viewer/state_stream.py) to poll /state and /capabilities from
# inside the container without opening a direct network route to it.
requests==2.32.3
